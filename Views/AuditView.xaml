<UserControl x:Class="CamperoDesktop.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.InputBindings>
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding BuscarCommand}"/>
    </UserControl.InputBindings>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,0,18">

            <ProgressBar Height="4"
                         IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                         Margin="0,0,0,8"/>

            <Border Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Buscar" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox Grid.Row="1" Margin="0,6,12,0" Height="34" Text="{Binding Buscar, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Tabla" Grid.Column="1" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,6,12,0" Height="34"
                              ItemsSource="{Binding TablasFiltro}"
                              SelectedItem="{Binding FiltroTabla}">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate><StackPanel/></ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                    </ComboBox>

                    <TextBlock Text="Accion" Grid.Column="2" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox Grid.Row="1" Grid.Column="2" Margin="0,6,12,0" Height="34"
                              ItemsSource="{Binding AccionesFiltro}"
                              SelectedItem="{Binding FiltroAccion}"/>

                    <TextBlock Text="Desde" Grid.Column="3" Style="{StaticResource FieldLabelStyle}"/>
                    <DatePicker Grid.Row="1" Grid.Column="3" Margin="0,6,12,0" Height="34"
                                SelectedDate="{Binding FiltroDesde}"/>

                    <TextBlock Text="Hasta" Grid.Column="4" Style="{StaticResource FieldLabelStyle}"/>
                    <DatePicker Grid.Row="1" Grid.Column="4" Margin="0,6,12,0" Height="34"
                                SelectedDate="{Binding FiltroHasta}"/>

                    <StackPanel Grid.Row="1" Grid.Column="5" Orientation="Horizontal" Margin="12,6,0,0" VerticalAlignment="Bottom">
                        <Button Content="Buscar" Width="90" Height="34" Command="{Binding BuscarCommand}"/>
                        <Button Style="{StaticResource SecondaryButtonStyle}" Content="Limpiar" Width="90" Height="34" Margin="8,0,0,0" Command="{Binding LimpiarFiltrosCommand}"/>
                        <Button Style="{StaticResource DangerButtonStyle}" Content="Limpiar Antiguos" Width="130" Height="34" Margin="8,0,0,0" Command="{Binding LimpiarAntiguosCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,18,0,0" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="Registros de Auditoria" FontSize="16" FontWeight="SemiBold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Estado}" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
                    </DockPanel>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Logs}"
                              SelectedItem="{Binding SelectedLog}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserSortColumns="False"
                              Margin="0,0,0,12">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                            <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120"/>
                            <DataGridTextColumn Header="Tabla" Binding="{Binding Tabla}" Width="110"/>
                            <DataGridTextColumn Header="Accion" Binding="{Binding Accion}" Width="90"/>
                            <DataGridTextColumn Header="Registro ID" Binding="{Binding RegistroId}" Width="90"/>
                            <DataGridTextColumn Header="Descripcion" Binding="{Binding Descripcion}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <Button Content="&lt;" Width="40" Height="30" Command="{Binding PaginaAnteriorCommand}"/>
                        <TextBlock Text="{Binding PaginaActual}" VerticalAlignment="Center" Margin="12,0" FontSize="14" FontWeight="SemiBold"/>
                        <TextBlock Text="/" VerticalAlignment="Center" Margin="0,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBlock Text="{Binding TotalPaginas}" VerticalAlignment="Center" Margin="12,0,12,0" FontSize="14" Foreground="{StaticResource MutedTextBrush}"/>
                        <Button Content="&gt;" Width="40" Height="30" Command="{Binding PaginaSiguienteCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,18,0,0" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Detalle del Registro" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <TextBox Grid.Row="1"
                             Text="{Binding DetalleJson, Mode=OneWay}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             Background="{StaticResource PanelAltBrush}"
                             BorderThickness="0"
                             Padding="12"
                             MinHeight="180"/>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
